---
description: run the jekyll project locally
---

To run the project locally with live reload:

// turbo
1. Run the jekyll server:
   ```bash
   bundle exec jekyll serve --livereload
   ```
2. Open your browser to `http://127.0.0.1:4000/`
